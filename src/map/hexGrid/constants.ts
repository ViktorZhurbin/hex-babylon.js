//The math and properties for creating the hex grid.
const GRID_SIDE = 4;
const GRID_ROWS = GRID_SIDE * 2;

const HEX_LENGTH = 1;
const HEX_WIDTH = Math.sqrt(3) * HEX_LENGTH;
const HEX_HEIGHT = 2 * HEX_LENGTH;

export { GRID_ROWS, GRID_SIDE, HEX_HEIGHT, HEX_LENGTH, HEX_WIDTH };
