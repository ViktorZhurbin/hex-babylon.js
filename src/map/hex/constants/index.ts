const HEX_LENGTH = 1;
const HEX_WIDTH = Math.sqrt(3) * HEX_LENGTH;

const Hex = {
  Height: 2 * HEX_LENGTH,
  Length: HEX_LENGTH,
  Radius: HEX_WIDTH / 2,
  Rotation: 150,
  Width: HEX_WIDTH,
};

export { Hex };
